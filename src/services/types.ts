export interface ArchiveEntry {
  id: string;
  title: string;
  content: string;
  category: string;
  date?: string;
  readTime?: string;
  excerpt?: string;
  summary?: string;
  tags?: string[];
}
